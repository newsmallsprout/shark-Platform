# middleware-system（K8s 清单）

YAML 说明与分步操作已统一写入：

**[docs/deployment/README.md](../../../docs/deployment/README.md)**（文中「Traffic 中间件（Kubernetes）」章节）

本目录仅保留清单文件（`*.yaml`），便于 `kubectl apply -f infra/kubernetes/middleware-system/...`。
