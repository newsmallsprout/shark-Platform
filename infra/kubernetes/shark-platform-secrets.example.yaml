# Template only: replace PLACEHOLDER_* values (or use kubectl create secret ... --from-literal).
# Do not commit a file with real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: shark-platform-secrets
  namespace: default
type: Opaque
stringData:
  DJANGO_SECRET_KEY: "PLACEHOLDER_generate_with_openssl_rand_base64_48"
  MYSQL_PASSWORD: "PLACEHOLDER_strong_mysql_password"
  MONGO_PASSWORD: "PLACEHOLDER_strong_mongo_password"
